# PRAHARI potato-leaf disease classifier — Python training/eval/export deps.
# Install into a fresh virtual environment on a machine that has the dataset and
# (ideally) a GPU. These are NOT needed by the Next.js app — serving uses onnxruntime-node.
#
#   python -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
#   pip install -r ml/requirements.txt
#
# torch/torchvision: install the build that matches your platform/CUDA.
# CPU-only example:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA 12.1 example: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.2,<3
torchvision>=0.17,<1
onnx>=1.15
onnxruntime>=1.17
pillow>=10.0
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
